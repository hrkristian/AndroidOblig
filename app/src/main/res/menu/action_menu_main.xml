<?xml version="1.0" encoding="utf-8"?>
<!-- Dev info, fjernes før levering

 Opprettet 22. Feb - Kristian

-->
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!--
        Skal starte en søke-aktivitet
        Gjør ingenting. Må ha binds i manifest.xml, m.m.
     -->
    <item android:id="@+id/mainSearch"
        android:title="@string/main_menu_search"
        android:icon="@android:drawable/ic_menu_search"
        app:showAsAction="collapseActionView|always"
        app:actionViewClass="android.support.v7.widget.SearchView"
    />
    <!-- todo- nytt navn på onClick-metoden -->
    <item android:id="@+id/spawnButton_Activity1"
        android:title="@string/main_menu_activity1"
        app:showAsAction="ifRoom"
        android:onClick="spawnActivity1"
    />
    <!-- todo- nytt navn på onClick-metoden -->
    <item android:id="@+id/spawnButton_Fragment1"
        android:title="@string/main_menu_fragment1"
        app:showAsAction="ifRoom"
        android:onClick="spawnFragment1"
    />

</menu>